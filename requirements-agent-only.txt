# Agent + Snowflake only (no PDF load). Use when you run only:
#   scripts/ask_books.py, mistral_snowflake_agent.py (Cortex COMPLETE), snowflake_retriever.py, snowflake_helper.py
# Much smaller venv (~100–200 MB vs ~8 GB with unstructured[pdf]).

snowflake-connector-python>=3.18
langchain-core>=0.3
pandas>=2.0
python-dotenv>=1.2
